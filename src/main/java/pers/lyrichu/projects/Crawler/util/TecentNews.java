package pers.lyrichu.projects.Crawler.util;

public class TecentNews extends News {
    public TecentNews(String title,String url,String imgurl,String source) {
        super.setTitle(title);
        super.setUrl(url);
        super.setImgurl(imgurl);
        super.setSource(source);
    }
}
